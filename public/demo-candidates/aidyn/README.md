# Aidyn — demo candidate assets

- **`preview-poster.svg`** — static thumbnail used on the committee review page (no MP4 in the repo).
- Full-screen recordings can be linked later via applicant `videoUrl` or your own hosting; transcript remains the qualitative source for scoring.

Do **not** commit large raw video files here — use Git LFS or external URLs if needed.
